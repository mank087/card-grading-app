# Google Ads Editor import — DCM Grading, Aug 25 2026

Generated by `scripts/google-ads-editor-import.mjs` from the Editor whole-account export.
Import the files **in numeric order**. Review every diff in Editor before you Post.

## Before you import (manual, 5 minutes)

1. In Editor, **Account > Export > Export whole account** once more as a backup (you already have the Aug 25 one).
2. Remove the bare `free` negative keyword from **Search - Pokemon** and **Search-sports** (Keywords, negative > filter on `free`). File 02 adds the narrower replacements.
3. Move enterprise assets off the account level. In **Ad assets (extensions)** filter *Account level* and delete these callouts: `Your Logo On The Slab`, `400 Grades Monthly`, `In-Store Grading`, `From $0.40/Card`; the sitelink `See A Live Storefront`; and the structured snippet `Brands: Your logo on the slab`. File 03 re-adds them at the campaign level on the two enterprise campaigns only.
4. Delete the duplicate account-level sitelinks (`Pricing Plans` x2, `Grade Your First Card` x2) and fix `Market Pricing` to `https://www.dcmgrading.com/market-pricing`. File 03 supplies a clean consumer set at campaign level, so you can remove all account-level sitelinks if you prefer one source of truth.

## Import order (Account > Import > From file)

| File | What it does | Check in the import preview |
|---|---|---|
| `01-campaign-settings.csv` | Search Partners off on all Search; CPC cap $1.00 on General; **PMax tROAS 43% -> 250%** and Final URL expansion off; tracking template on every campaign; creates **Search-enterprise** (paused, $15/day, Max clicks, $2.50 cap). | Five campaign rows, one new campaign. If Editor asks about the Target ROAS format, type `250` in the field after import. |
| `01b-campaign-locations.csv` | United States for the new enterprise campaign. | One row. |
| `02-negative-keywords.csv` | General gets the shared hygiene list (59 phrase) + category routing (sports/Pokemon terms) + the Pokemon and Sports campaigns' keywords as **exact** negatives so category searches route to the specific campaigns. Pokemon/Sports get each other's category terms and the narrower `free` replacements. Enterprise gets jobs/psa/app/free etc. | 219 new negatives, no deletions. |
| `03a-sitelinks-campaign-level.csv` | 6 consumer sitelinks on each Search campaign; 4 enterprise sitelinks on Search-enterprise and Enterprise-PMAX. | 26 rows. |
| `03b-callouts-campaign-level.csv` | 8 consumer callouts per Search campaign; 6 enterprise callouts on the two enterprise campaigns. | 36 rows. |
| `04a-ad-group-settings.csv` | Creates 22 themed ad groups (Max CPC $0.60 consumer, $2.00 enterprise). | 22 new ad groups. |
| `04-ad-groups-and-keywords.csv` | Moves every existing keyword into its themed ad group (same match type, paused stays paused) and adds 60 enterprise keywords (exact + phrase). | 389 keyword rows. Editor will show them as **new** keywords in new ad groups. |
| `05-responsive-search-ads.csv` | One RSA per ad group, 14-15 headlines, 4 descriptions, the two theme headlines pinned to position 1. Landing pages: /pokemon-grading, /sports-grading, /grade-your-first-card (General), /enterprise. | 22 ads. Editor's Ad strength column should read Good/Excellent after import. |
| `04b-pause-old-ad-groups.csv` | Pauses the three original `Ad group 1` groups (their keywords now live in the themed groups). Import this **last**, after 04 and 05 are in and reviewed. | 3 rows changed to Paused. |

Then **Post** (Account > Post). Post 01-03 first if you want to land the low-risk fixes today and review the restructure separately.

## After posting (Google Ads UI, 10 minutes)

1. **Conversions:** Goals > Conversions > New conversion action > Import > Google Analytics 4 > Web. Import `purchase` as **Primary** (value from event, one per click) and `sign_up` as **Secondary**. The site already fires these events (GA4 property G-YLC2FKKBGC, credit and Card Lovers success pages).
2. **PMax goals:** open Sales-Performance Max - ALL > Settings > Conversion goals and set Purchase as the only primary goal (Signup secondary).
3. **Auto-tagging** stays on (account setting). The tracking template adds `utm_*` for the site's own analytics.
4. Enable **Search-enterprise** when you are ready; consider pausing Enterprise-PMAX for now ($10/day is below what PMax needs to learn).
5. After ~30 purchases in 30 days, switch Search - Pokemon and Search-sports to **Maximize conversions**, then add a target CPA.

## What changed in the copy and why

- Every RSA leads with the ad group's intent (pinned), then the eight benefits from `marketing/BENEFITS_MESSAGING.md`: about a minute, no shipping, subgrades and centering, real sold-listing pricing, printable labels, list to eBay, image confidence A-D, from $0.50.
- Removed: `From Raw to Riches` (resale framing) and `30-Point Condition Check` (unverifiable count). No "pre-grade / pre-check / second opinion / know before you go" anywhere in ad copy (retired positioning per `PLATFORM_ADS_COPY.md`); the keywords that carry that intent stay, the ads answer them with "Grade it yourself in a minute" / "Full grading, not a guess".
- No grader names in copy. The existing account-level callout `PSA/BGS/SGC/CGC Estimates` was left as-is (factual, not disparaging); remove it if you want zero competitor mentions.
- Enterprise copy mirrors the PMax asset group already approved (logo on the slab, $0.40/card, $199/mo for 400, branded verification page).
- Pricing claims: $0.50 per grade = Card Lovers annual (900 for $449); $0.40 = Enterprise 1,000 for $399. If either plan changes, regenerate.

## Round 2 — Performance Max optimisation (files 06, 06b, 07)

Generated by `scripts/google-ads-pmax-import.mjs`. Applies to **Sales-Performance Max - ALL** only (Enterprise-PMAX is paused and untouched).

| File | What it does |
|---|---|
| `06-pmax-asset-groups.csv` | Creates three asset groups, **Pokemon**, **Sports**, **TCG & Collectibles**, each with its own landing page (/pokemon-grading, /sports-grading, /grade-your-first-card), 15 headlines, 5 long headlines, 5 descriptions, business name, "Sign up" CTA, the two existing YouTube videos, and audience signals: the saved audiences `Pokémon Audience` / `Sports Audience` / `Website Audience - copy` plus the matching custom segments (`Pokemon Cards;Trading Card Games`, `Sports Memorabilia`, `Trading Card Games`). |
| `06b-pmax-search-themes.csv` | ~20 search themes per asset group (category-specific; Google's cap is 25). |
| `07-campaign-fixes.csv` | The settings the first import did not apply: Search Partners off on Search - Pokemon and Search-sports; Search-enterprise set to $15/day, Maximize clicks with a $2.50 cap, Google search only, Paused. |

**Import order:** 07, then 06, then 06b. Review each preview; Post.

**After import, in Editor or the Ads UI (images cannot be set by CSV):**
1. Open each new asset group and attach images and the logo from the account's asset library (33 images are already uploaded: the DCM logos at 1024×1024, the 1:1 and 1.91:1 creatives). Each group needs at least 3 landscape (1.91:1), 3 square (1:1), 1 portrait (4:5) and the logo; use the Pokemon and Sports creatives from `marketing/pmax-consumer` where they exist.
2. Campaign **Settings → Conversion goals**: make **Purchase** the only primary goal; set Signup to secondary. (The campaign currently bids to both equally, so free signups count as much as credit purchases.)
3. Leave the original **Asset Group 1** enabled for two weeks while the new groups exit Learning, then pause it. Do not delete it: its history helps the campaign.
4. The tROAS is now 250%. If the campaign's conversion volume drops sharply in week one, drop it to 180% for two weeks and step back up; PMax needs ~30 conversions in 30 days to hold a target.

## Regenerate

```
node scripts/google-ads-editor-import.mjs "<path to Editor export>.csv"
```

Edit the `THEMES`, `rsa(...)`, sitelink and callout arrays in the script; it validates character limits and banned phrases before writing.
