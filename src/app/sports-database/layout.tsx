import { publicPageMetadata } from '@/lib/seo/pageMetadata'

export const metadata = publicPageMetadata("/sports-database", "Sports Card Database: Browse Sets and Players", "Browse sports card sets and players, review available card details and market prices, and choose a card to grade with DCM Optic.")

export default function Layout({ children }: { children: React.ReactNode }) { return children }
