import { privatePageMetadata } from '@/lib/seo/pageMetadata'

export const metadata = privatePageMetadata("Email Preferences")

export default function Layout({ children }: { children: React.ReactNode }) { return children }
