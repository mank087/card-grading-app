import { MarketingShowcaseBoundary } from '@/components/marketing/MarketingShowcaseBoundary'
export default function Layout({children}: {children: React.ReactNode}) {return <MarketingShowcaseBoundary selection="ai-card-grading">{children}</MarketingShowcaseBoundary>}
