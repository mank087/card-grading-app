import { privatePageMetadata } from '@/lib/seo/pageMetadata'

export const metadata = privatePageMetadata("Prepare Listing Images")

export default function Layout({ children }: { children: React.ReactNode }) { return children }
