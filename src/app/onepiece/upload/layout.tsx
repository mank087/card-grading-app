import { privatePageMetadata } from '@/lib/seo/pageMetadata'

export const metadata = privatePageMetadata("Upload One Piece Cards")

export default function Layout({ children }: { children: React.ReactNode }) { return children }
